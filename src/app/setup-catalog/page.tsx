import { SetupCatalogView } from "@/components/setup-catalog/SetupCatalogView"

export const metadata = {
  title: "Setup Catalog | AI Edge",
  description:
    "Example trades for every Al Brooks setup, traded by scaling in behind a wide stop.",
}

export default function SetupCatalogPage() {
  return <SetupCatalogView />
}
