import { WedgesView } from "@/components/wedges/WedgesView"

export const metadata = {
  title: "Wedges — AI Edge",
  description:
    "Study gallery of Al Brooks three-push wedge reversals — detected, charted, and honestly backtested.",
}

export default function WedgesPage() {
  return <WedgesView />
}
