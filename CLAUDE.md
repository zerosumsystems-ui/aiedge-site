@AGENTS.md

# Claude Notes

Use the repo-wide `AGENTS.md` instructions as the source of truth.

When Will says `go`, `/go`, `go live`, or asks to ship current work, follow the Go Live Workflow in `AGENTS.md` and `.claude/commands/go.md`: quality check, show proof, publish to `main`, wait for Vercel Production, then verify the live URL.
